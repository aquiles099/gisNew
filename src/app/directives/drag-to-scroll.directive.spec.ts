import { DragToScrollDirective } from './drag-to-scroll.directive';

describe('DragToScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new DragToScrollDirective();
    expect(directive).toBeTruthy();
  });
});
